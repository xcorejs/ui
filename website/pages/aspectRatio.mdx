import Header from "../components/Header";
import PropsTable from "../components/PropsTable";
import { Button, Stack, AspectRatio } from "@xcorejs/ui";
import XcoreIcon from "../components/icons/16/xcore.svg";

<Header
  title="Aspect Ratio"
/>

Component used for making aspect ratio Boxes. It's good when you need to use embed iframe or just responsive images and videos.
It is based on common [padding hack](https://css-tricks.com/aspect-ratio-boxes/).

<AspectRatio ratio={9/16} bg="primary" />

```jsx
import { Icon } from "@xcorejs/ui";

<AspectRatio ratio={9/16} bg="primary" />
```

## Props

### AspectRatioProps

Extends: Box

<PropsTable
  props={[
    ["ratio", "number", null, "Ratio - height/width"],
  ]}
/>
